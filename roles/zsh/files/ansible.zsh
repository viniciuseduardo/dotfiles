#!/bin/bash

alias avv="ansible-vault --vault-password-file=${HOME}/.vault view "
alias avc="ansible-vault --vault-password-file=${HOME}/.vault create "
alias ave="ansible-vault --vault-password-file=${HOME}/.vault edit "
